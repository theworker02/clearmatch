{
  "name": "clearmatch-workspace",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:python\" \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "dev:python": "python -m uvicorn app:app --app-dir python-service --reload --port 8000",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace frontend",
    "prisma:generate": "npm run prisma:generate --workspace backend",
    "prisma:push": "npm run prisma:push --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "agent-browser": "^0.26.0"
  }
}
