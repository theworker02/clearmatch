# Target

This folder is reserved for local release artifacts, exported screenshots, packaged builds, and temporary QA output.

Generated files inside `target/` are ignored by Git. Keep source code, docs, and hand-authored release notes in the project root or `docs/`.
